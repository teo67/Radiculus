dig operators plant import "operators"
dig functions plant import "functions"

dig x plant new operators.Variable("x")
dig y plant new operators.Variable("y")
dig myFunction plant new operators.Product([new operators.Power(x, new operators.Constant(2)), new operators.Power(y, new operators.Constant(2))])
holler(myFunction.print())
holler(myFunction.differentiate(x).print())